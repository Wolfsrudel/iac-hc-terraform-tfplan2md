## Pre-PR Checklist Skill

Adds a new `pre-pr-checklist` agent skill and mandates its use before every PR creation or push. The skill provides an ordered checklist — work item folder creation, formatting, build, tests, snapshot token, demo regeneration, commit hygiene, and local guardrail scripts — ensuring single-agent tasks satisfy the same minimum requirements as full multi-agent workflows.
